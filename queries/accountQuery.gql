query ACCOUNT_BY_ID($id: String!) {
  accountByIdQuery: account(id: $id) {
    id
    numChallenges
    challenges {
      id
      title
      description
      videos {
        id
      }
    }
    totalFund
    wins
  }
}
